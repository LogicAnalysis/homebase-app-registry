---
services:
  filebrowser:
    image: gtstef/filebrowser:1.4-stable
    container_name: filebrowser
    user: "${UID}:${GID}"
    volumes:
      - /app/data/apps/storage:/storage
      - /app/data/apps/media:/media
      - ./data:/home/filebrowser/data
      - ./files:/files
    ports:
      - 8900:80
    restart: unless-stopped
