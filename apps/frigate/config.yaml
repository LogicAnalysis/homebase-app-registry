auth:
  enabled: False
mqtt:
  enabled: False
  host: localhost
tls:
  # Optional: Enable TLS for port 8971 (default: shown below)
  enabled: False
cameras:
  fake_camera: # Fake camera to allow Frigate to start
    ffmpeg:
      inputs:
        - path: "rtsp://fake-url"
          roles:
            - detect